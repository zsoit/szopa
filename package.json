{
    "dependencies": {
        "jstransformer-coffee-script": "^1.1.1",
        "jstransformer-markdown-it": "^2.1.0",
        "pug": "^3.0.2"
    },
    "scripts": {
        "dev": "rm -rf dist/*  && parcel src/index.pug src/404.pug",
        "github": "git add . && git commit -m 'update' && git push",
        "b": "rm -rf public_html/*   && parcel build src/index.pug src/404.pug --dist-dir public_html && cp -R src/.htaccess public_html/.htaccess"

    },
    "devDependencies": {
        "@parcel/transformer-pug": "^2.0.1",
        "babel-plugin-react-transform": "^2.0.0-beta1",
        "parcel": "^2.0.1"
    }
}